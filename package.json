{
  "name": "upsc-saga",
  "version": "1.0.0",
  "scripts": {
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm run build && cp -r dist ../"
  },
  "devDependencies": {
    "terser": "^5.44.1"
  }
}
