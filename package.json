{
  "scripts": {
    "build": "cd frontend && pnpm install && pnpm run build && cd .. && rm -rf dist && cp -r frontend/dist dist"
  },
  "dependencies": {
    "typescript": "^5.2.2",
    "vite": "^6.4.1"
  },
  "devDependencies": {
    "terser": "^5.44.1"
  }
}
