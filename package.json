{
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build && cd .. && rm -rf dist && cp -r frontend/dist dist && echo \"Build done\""
  },
  "devDependencies": {
    "terser": "^5.44.1"
  }
}
