{
  "devDependencies": {
    "terser": "^5.44.1"
  },
  "scripts": {
    "build": "cd frontend && pnpm install --frozen-lockfile=false && pnpm run build && cd .. && rm -rf dist && cp -r frontend/dist dist"
  }
}
