{
  "name": "upsc-saga",
  "version": "1.0.0",
  "description": "UPSC Second Brain",
  "scripts": {
    "postinstall": "cd frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "terser": "^5.44.1"
  }
}
