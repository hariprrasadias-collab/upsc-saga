{
  "scripts": {
    "build": "cd frontend && pnpm install --prod=false && pnpm run build && cd .. && rm -rf dist && cp -r frontend/dist dist"
  },
  "devDependencies": {
    "terser": "^5.44.1"
  }
}
